name: CI and Deployment

on:
    push:
        branches:
            - master

jobs:
    test-and-build:
        name: Test and Build
        runs-on: ubuntu-latest
        if: "contains(github.event.head_commit.message, '-release-')"

        steps:
            - name: Repository checkout
              uses: actions/checkout@v2

            - name: Use NodeJS 12.x
              uses: actions/setup-node@v1
              with:
                  node-version: 12.x

            - name: Install dependencies
              run: npm ci

            - name: Test
              run: npm run test:ci
              env:
                  CI: true
                  GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}

            - name: Build
              run: npm run build:ci
              env:
                  GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}

            - name: Archive build
              uses: actions/upload-artifact@v2
              with:
                  name: build
                  path: build

            - name: Archive code coverage result
              uses: actions/upload-artifact@v2
              with:
                  name: coverage
                  path: coverage
    deploy:
        name: Deploy
        runs-on: ubuntu-latest
        needs: test-and-build
        steps:
            - name: Repository checkout
              uses: actions/checkout@v2

            - name: Download build
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: build

            # - name: Deploy to Heroku
            #   shell: bash
            #   env:
            #       heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
            #       heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
            #       heroku_email: ${{ secrets.HEROKU_EMAIL }}
            #       heroku_username: ${{ secrets.HEROKU_USERNAME }}
            #   run: |
            #       chmod u+w ~
            #       chmod +rx ./.scripts/deploy.sh
            #       ./.scripts/deploy.sh

            - name: Deploy to Heroku
              uses: akhileshns/heroku-deploy@v3.5.6
              with:
                  heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
                  heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
                  heroku_email: ${{ secrets.HEROKU_EMAIL }}

            # - name: Deploy to Heroku
            #   uses: cpina/github-action-push-to-another-repository@master
            #   env:
            #       HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
            #       HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
            #       HEROKU_EMAIL: ${{ secrets.HEROKU_APP_NAME }}
            #   with:
            #       source-directory: 'build'
            #       destination-github-username: ${{ secrets.HEROKU_EMAIL }}
            #       destination-repository-name: ${{ secrets.HEROKU_APP_NAME }}
            #       user-email: ${{ secrets.HEROKU_EMAIL }}

            # - name: Deploy to Heroku
            #   env:
            #       HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
            #       HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
            #   run: |
            #     heroku
            # run: |
            #     git clone https://git.heroku.com/$HEROKU_APP_NAME.git
            #     rm -r $HEROKU_APP_NAME/dist
            #     cp -r build/dist/* $HEROKU_APP_NAME/dist
            #     git add .
            #     git commit "Release"
            #     git push --force https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git master
